[alias]
	sync = !git pull origin $(git branch | grep \"^*\" | sed \"s/* //\") && git push origin $(git branch | grep \"^*\" | sed \"s/* //\")
[color]
	ui = auto
[user]
	name = Samuel Cole
	email = sam@samuelcole.name
[merge]
	log = true
